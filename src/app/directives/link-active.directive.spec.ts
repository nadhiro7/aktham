import { LinkActiveDirective } from './link-active.directive';

describe('LinkActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkActiveDirective();
    expect(directive).toBeTruthy();
  });
});
